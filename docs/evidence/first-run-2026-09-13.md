# 首次使用教程验证 · 2026-09-13

结论：从新的 main 克隆目录完成了配置与使用流程。用户确认应用后的 QQ 定位正常，三分钟到期后也回归。本轮在同一台 Mac、同一 Apple 账号及已有 iPhone 上完成，是受限条件下的端到端验证，不等同于全新用户环境认证。

## 起点与边界

旧仓库保留，但旧控制器安装已停用，旧目录的 direnv 授权已撤回，Pinshift 服务端配对凭据已删除。新克隆起初没有控制器安装和本地 Team 配置，Xcode 显示 Team=None。

Nix、Xcode、Apple 账号与开发证书、签名 profile、Pinshift TLS 身份以及手机 App/数据仍然存在。不能据此证明 Nix 首次安装、新 Apple 账号签名、不同 Bundle Identifier 或缺失 TLS 身份时的创建流程已通过。

## 已完成的步骤

| 步骤 | 结果与证据 |
| --- | --- |
| 项目环境入口 | 用户确认 direnv 加载及 `pinshift help` 正常；新仓库命令优先 |
| 选择 Team | Xcode 界面先显示 None 和签名错误；用户选择 Personal Team 后错误消失，已保存私有截图 |
| Xcode 首次运行 | 用户确认手机成功打开；Xcode 显示运行结束，已保存私有截图。这是新克隆的首次运行，手机并非首次安装 |
| Mac setup | 用户提供的完整输出显示构建完成、真实 Clear 回执、已有 TLS 身份授权及控制器安装成功 |
| Doctor | 工具链、设备、开发模式、设备镜像、签名及通信身份均 PASS；全局 Command Line Tools 与手机权限提示为 WARN，无失败项 |
| 前台启动 | 用户输出显示现存签名有效、跳过续签、Controller Link 就绪并提供限时配对码；配对码不公开保留 |
| 配对 | 撤销旧服务端凭据后，用户确认重新连接成功 |
| 应用与到期 | 用户按流程验证后确认全部正常、三分钟后回归；包括本次 QQ 定位体验。没有独立捕获这一轮的定位样本、误差或精确解除时序，不据此承诺米级精度或固定恢复延迟 |

## 图解素材与尚未覆盖项

已保存两张 Xcode 原图及 setup 文本，原图含个人签名信息，仅留本机，公开前须脱敏。其余步骤目前以用户文本反馈为证据，尚无完整逐步截图，不制作伪造的操作截图。

本机注册入口在临时目录完成了真实 Nix 调用帮助和卸载检查，但用户本轮使用的是项目内入口，未完成正式全局入口的应用/解除流程。锁屏、睡眠、失联、进程退出等分支没有在本轮重测。

教程入口：[首次使用与配置](../getting-started.md) · [Apple 签名](../apple-signing.md)。
