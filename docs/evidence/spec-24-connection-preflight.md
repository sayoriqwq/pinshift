# #24 真机连接与安装验收

2026-09-12，本人将本轮范围从“禁止使用 iPhone”更新为“可以真机验收，现在检测连接情况”。
先对既有稳定控制器做只读预检，随后验证本次版本的安装与连接。以下不是全功能验收通过报告。

| 检查 | 结果 |
| --- | --- |
| 唯一已配对物理 iPhone | 已发现，Xcode 设备工作流可达 |
| Developer Mode | 可用 |
| Xcode 设备支持镜像 | 兼容 |
| 开发签名身份 | 可用 |
| Keychain 中的 Controller Link 身份 | 就绪 |
| 手机上的 Pinshift App | 已安装；检查时未运行 |
| 手机解锁状态 | 本次启动后已解锁，检查时无需输入密码 |
| Xcode 首次初始化 | 初次失败；安装 Xcode 官方必需系统组件后复检通过 |
| 全局开发者目录 | 仍指向 Command Line Tools；命令显式使用 Xcode Beta，不影响本次检查 |

复检 `pinshift doctor` 返回成功。预检未发送 Apply、Clear 或启动控制器会话。
App 内的定位／局域网权限、实际 Bonjour/TLS 连接与本次版本的签名安装需另行验证。
精度、到期解除、锁屏／失联恢复和逐 App 传播仍按 [本人验收清单](spec-24-owner-acceptance.md)
执行；#21 和 #23 保持开放。

设备标识、签名身份细节与原始设备输出不纳入公开记录。

## 本次版本安装与会话启动

随后在验收工作区完成了以下操作。构建时的生产源码与 `0186c3d` 一致；后续合并仅调整 UI 测试。

| 操作 | 结果 |
| --- | --- |
| `pinshift setup` | Release 构建、稳定签名验证和控制器安装成功；既有 Keychain 身份保留并授权 |
| setup 中的真实 Clear | 后端成功回执；未据此推断新的物理位置观测已经到达 |
| `pinshift app --force` | 新签名校验通过，手机原位安装成功，签名有效至 2026-09-19；未卸载既有 App |
| 自动打开 App | 最初被手机锁屏阻止；本人解锁打开后，进程检查确认 App 运行 |
| 单命令 `pinshift` | 正确跳过无须重复的续签；真实启动清理成功，控制器进入 ready |

本人随后确认 App“更多”显示“已连接”。2026-09-12 21:04 CST，Mac 诊断连续记录了
`controller.link.request.received`（status）、命令完成（idle）及响应发送（idle）。
这些 status 响应仅在授权校验通过后生成；结合手机端显示，确认本次版本两端连接通过。

自动连接检查没有发起 Apply。随后会话实际收到了一次 Apply，并在 21:04:52 CST 记录后端成功回执。
21:06:26 CST 正常退出开始清理，21:06:27 CST 取得真实 Clear 成功回执，进程以状态 0 退出。
本次没有对手机新鲜观测或其他 App 做对照，也没有等待完整三分钟；不将这些回执记为精度或到期验收通过。

验证会话已正常结束。已提供验收目录中的 `./bin/pinshift` 命令，让本人在可直接控制的终端中继续。
三分钟到期、替换、定位精度、锁屏／失联恢复与逐 App 传播仍需分别验收。
