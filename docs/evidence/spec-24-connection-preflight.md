# #24 真机连接预检

2026-09-12，本人将本轮范围从“禁止使用 iPhone”更新为“可以真机验收，现在检测连接情况”。
以下是既有稳定控制器的只读预检结果，不是本次 PR 全功能验收通过报告。

| 检查 | 结果 |
| --- | --- |
| 唯一已配对物理 iPhone | 已发现，Xcode 设备工作流可达 |
| Developer Mode | 可用 |
| Xcode 设备支持镜像 | 兼容 |
| 开发签名身份 | 可用 |
| Keychain 中的 Controller Link 身份 | 就绪 |
| 手机上的 Pinshift App | 已安装；检查时未运行 |
| 手机解锁状态 | 本次启动后已解锁，检查时无需输入密码 |
| Xcode 首次初始化 | 初次失败；安装 Xcode 官方必需系统组件后复检通过 |
| 全局开发者目录 | 仍指向 Command Line Tools；命令显式使用 Xcode Beta，不影响本次检查 |

复检 `pinshift doctor` 返回成功。预检未发送 Apply、Clear 或启动控制器会话。
App 内的定位／局域网权限、实际 Bonjour/TLS 连接与本次版本的签名安装需另行验证。
精度、到期解除、锁屏／失联恢复和逐 App 传播仍按 [本人验收清单](spec-24-owner-acceptance.md)
执行；#21 和 #23 保持开放。

设备标识、签名身份细节与原始设备输出不纳入公开记录。
