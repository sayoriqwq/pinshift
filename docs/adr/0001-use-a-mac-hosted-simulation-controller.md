# Use a Mac-hosted simulation controller

The Pinshift app chooses a Selected Location; a Mac-hosted Simulation Controller applies it through Xcode to the developer’s paired iPhone. This keeps device-level injection outside the iOS app and uses Apple’s development workflow. Selected, Applied and Verified remain distinct: backend acknowledgement does not prove a fresh app observation, and Cross-App Propagation is measured per app without bypassing rejection of simulated input.
